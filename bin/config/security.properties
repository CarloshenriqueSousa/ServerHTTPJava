# Vaultra Secure Server - Configurações de Segurança
# ===================================================

# Hash SHA-256 do token mestre (será preenchido na primeira execução)
master.token.hash=

# Rotas públicas (sem autenticação). Separadas por vírgula.
# Use /* no final para wildcard: /public/* aceita /public/qualquercoisa
public.routes=/*

# Limite máximo de requisições por minuto por IP
max.requests.per.minute=60
